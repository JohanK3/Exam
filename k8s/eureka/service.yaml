apiVersion: v1
kind: Service
metadata:
  name: eureka-service
  namespace: exam-namespace
  annotations:
    metallb.universe.tf/address-pool: lb-ipaddresspool
spec:
  selector:
    app: eureka-service
  type: LoadBalancer
  ports:
    - protocol: TCP
      port: 8761
      targetPort: 8761
