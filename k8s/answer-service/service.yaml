apiVersion: v1
kind: Service
metadata:
  name: answer-service
  namespace: default
spec:
  selector:
    app: answer-service
  ports:
    - protocol: TCP
      port: 8083
      targetPort: 8083
  type: ClusterIP