apiVersion: v1
kind: Service
metadata:
  name: api-gateway-service
spec:
  type: NodePort
  selector:
    app: api-gateway-service
  ports:
    - protocol: TCP
      port: 8090
      targetPort: 8090
      nodePort: 30080
