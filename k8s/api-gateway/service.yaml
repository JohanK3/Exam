apiVersion: v1
kind: Service
metadata:
  name: api-gateway-service
  namespace: exam-namespace
spec:
  selector:
    app: api-gateway-service # MODIFIÉ : Pour correspondre au nouveau label du déploiement
  ports:
    - protocol: TCP
      port: 8090
      targetPort: 8090
  type: ClusterIP
