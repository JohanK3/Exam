apiVersion: v1
kind: Service
metadata:
  name: course-service
  namespace: exam-namespace
spec:
  selector:
    app: course-service
  ports:
    - protocol: TCP
      port: 8081
      targetPort: 8081
  type: ClusterIP