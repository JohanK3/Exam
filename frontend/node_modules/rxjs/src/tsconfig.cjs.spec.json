{
  "extends": "./tsconfig.cjs.json",
  /**
   * this is pseudo config for test.jmx case's relative path resolution to `../src`, runs
   * cjs config once more instead of manual copying to utilize incremental build.
   */
  "compilerOptions": {
    "outDir": "../dist/src"
  }
}
